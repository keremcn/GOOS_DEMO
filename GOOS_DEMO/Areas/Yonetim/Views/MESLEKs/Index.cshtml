@model IEnumerable<GOOS_DEMO.Models.MESLEK>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Yonetim/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.M_ADI)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.M_ADI)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.M_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.M_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.M_ID })
        </td>
    </tr>
}

</table>
